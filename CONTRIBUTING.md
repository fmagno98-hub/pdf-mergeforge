# Contributing to PDF MergeForge

Use Python 3.12, create a local virtual environment, and run `pytest` plus `ruff check .` before opening a pull request. Do not commit personal PDFs, logs, build output, credentials, or generated executables.
